// Marker file — Revive OS publish-blog-to-site edge function checks for this
// to decide whether to publish blogs as native Next.js JSON (rendered by
// src/app/blog/b/[slug]/page.tsx) or fall back to orphan static HTML.
// Keep this file in place; empty export is sufficient as the signal.
export {};